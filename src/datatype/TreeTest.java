package datatype;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class TreeTest {

	@Test
	void test() {
		LowpanNode tmpNode = null;
		Tree<LowpanNode> network = new Tree<LowpanNode>(new LowpanNode("root", 256));
		
		
	}

}
